{
  "name": "xlayer-pumpfun-contracts",
  "version": "1.0.0",
  "description": "Pump.fun-style Meme Token Launchpad on X Layer",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:xlayer": "hardhat run scripts/deploy.js --network xlayer",
    "deploy:testnet": "hardhat run scripts/deploy.js --network xlayer-testnet",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@openzeppelin/contracts-upgradeable": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0"
  }
}